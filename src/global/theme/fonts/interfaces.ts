import { CSSProp } from 'styled-components';

export interface FontFormat {
    THIN: CSSProp;
    LIGHT: CSSProp;
    REGULAR: CSSProp;
    MEDIUM: CSSProp;
    SEMI_BOLD: CSSProp;
    BOLD: CSSProp;
}
